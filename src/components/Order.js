import React from 'react';
import {Header} from './Header';

export default class Order extends React.Component {
    render() {
        return (
            <div>
                <Header />
                
            </div>
        )
    }
}